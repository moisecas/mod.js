División, módulo y residuo en JavaScript
mod modulo, simbolizado por % 
Saber cuando el número no es divisible 
5%5=0
cuando el resutlado es = 0 es par, diferente a 0 es impar. 

Primer problema, imprimir los numeros 
var = 100, definimos la variable 
Creamos un for para mostrar los numeros y recorrerlos
for(var i = 1; i <= 100; i++)

En el document.write mostramos la variable, usando etiqueta de html podemos ver los numeros en columna. document.write("<br />");

En programación primero se ejecuta las operaciones matematicas y luego las comparaciones 

Defino dentro del ciclo donde voy a mostrar e resultado por medio del document.write()

Otra forma de hacerlo 
si el numero no es divisible por 3 y 5, imprimilao
if (i%3 !=0 && i%5!=0)




